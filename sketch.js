// xoff1 = 0;
// xoff2 = 10000;
let incr = 0.5;
let img;

colorImg = [];



  
function setup() {
    createCanvas(100, 100);
    
    
    
    
    
}

function draw() {

    
    // image(img, 0, 0);
    // let c = get(0,1);
    // console.log(c);



    // image(img, 0, 0);
    // let c = get();

    // loadPixels();
    // yoff = 0;
    // for (let y = 0 ; y < height ; y++){
    //     xoff = 0;
    //     for (let x = 0 ; x < width ; x++){
    //         var index = (x + y * width) * 4;
    //         const [r, g, b] = [pixels[index], pixels[index + 1], pixels[index+ 2]]; // get colors
    //         colorImg.push(r);
    //         colorImg.push(g);
    //         colorImg.push(b);
    //         // console.log(colorImg)

    //         var n1 = map(noise(xoff, yoff),0,1,0,r/2);
    //         var n2 = map(noise(xoff, yoff),0,1,0,g/2);
    //         var n3 = map(noise(xoff, yoff),0,1,0,b/2);
    //         pixels[index + 0 ] = r + n2;
    //         pixels[index + 1 ] = g + n2;
    //         pixels[index + 2 ] = b + n3;
    //         pixels[index + 3 ] = 255;

    //         xoff += incr;
    //     }
    //     yoff += incr;
    // }
    // updatePixels();
}